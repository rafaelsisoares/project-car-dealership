# Configuracao do DataSource
spring.datasource.url=jdbc:mysql://localhost:3306/dealershipdb?createDatabaseIfNotExist=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=rootDealership
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuracao do Hibernate
spring.jpa.hibernate.ddl-auto=update

# Configuracao da JPA
spring.jpa.show-sql=true
spring.application.name=project-car-dealership

authentication.jwt.secret=${JWT_SECRET:d34lER5h1P}
